# Runtime connection material. Install real values through the deployment
# secret mechanism; never commit them. There are deliberately no code defaults.
DOTMAC_CLOUD_DATABASE_URL=postgresql+psycopg://app_user:change-me@localhost:5432/dotmac_cloud
DOTMAC_CLOUD_MIGRATION_DATABASE_URL=postgresql+psycopg://app_admin:change-me@localhost:5432/dotmac_cloud
